# Local development overrides — do NOT commit real secrets
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mmucc5?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: mmucc_user
    password: P@ssword08

  flyway:
    baseline-on-migrate: true

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
mmucc:
  jwt:
    secret: bG9jYWwtZGV2LXNlY3JldC1rZXktbXVzdC1iZS0yNTYtYml0cw==
  cors:
    allowed-origins: http://localhost:4200

logging:
  level:
    gov.nhtsa.mmucc: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE
